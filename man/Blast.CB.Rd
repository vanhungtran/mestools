% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primers-16s.R
\name{Blast.CB}
\alias{Blast.CB}
\title{Run Local BLAST Search}
\usage{
Blast.CB(
  x,
  blastn = "blastn",
  blast_db = blast_db,
  input = x,
  evalue = 0.05,
  format =
    "\\"6 sscinames pident length qcovhsp mismatch gapopen qstart qend sstart send evalue bitscore\\"",
  max_target_seqs = 100,
  gapopen = 0,
  word_size = 28,
  qcov_hsp_perc = 90
)
}
\arguments{
\item{x}{Character. Path to query FASTA file}

\item{blastn}{Character. Path to blastn executable (default: "blastn")}

\item{blast_db}{Character. Path to BLAST database}

\item{input}{Character. Input file path (default: x)}

\item{evalue}{Numeric. E-value threshold (default: 0.05)}

\item{format}{Character. Output format specification}

\item{max_target_seqs}{Integer. Maximum number of aligned sequences to keep (default: 100)}

\item{gapopen}{Integer. Cost to open a gap (default: 0)}

\item{word_size}{Integer. Word size for wordfinder algorithm (default: 28)}

\item{qcov_hsp_perc}{Numeric. Minimum query coverage per HSP (default: 90)}
}
\value{
Tibble containing BLAST results
}
\description{
Executes a local BLAST search using blastn command and returns results as a tibble.
Requires NCBI BLAST+ to be installed and accessible in the system PATH.
}
\examples{
\dontrun{
blast_results <- Blast.CB(
  x = "sequences.fasta",
  blast_db = "path/to/16S_ribosomal_RNA"
)
}
}
