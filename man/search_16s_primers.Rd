% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primers-16s.R
\name{search_16s_primers}
\alias{search_16s_primers}
\title{Search and Filter 16S Primer Database}
\usage{
search_16s_primers(
  region = NULL,
  amplicon_size_min = NULL,
  amplicon_size_max = NULL,
  platform = NULL,
  coverage = NULL,
  bias_level = NULL,
  reference = NULL,
  keyword = NULL
)
}
\arguments{
\item{region}{Character. Filter by hypervariable region (e.g., "V4", "V3-V4")}

\item{amplicon_size_min}{Integer. Minimum amplicon size (bp)}

\item{amplicon_size_max}{Integer. Maximum amplicon size (bp)}

\item{platform}{Character. Filter by sequencing platform}

\item{coverage}{Character. Filter by coverage rating ("High", "Very High", etc.)}

\item{bias_level}{Character. Filter by bias level ("Low", "Very Low", etc.)}

\item{reference}{Character. Filter by publication/reference}

\item{keyword}{Character. Search in application or notes fields}
}
\value{
Filtered data.frame of primers matching criteria
}
\description{
Advanced search function to filter primers by multiple criteria including
region, amplicon size, platform, coverage, and reference.
}
\examples{
\dontrun{
# Find all V4 primers
v4_primers <- search_16s_primers(region = "V4")

# Find short amplicons for degraded DNA
short_primers <- search_16s_primers(amplicon_size_max = 300)

# Find primers optimized for Illumina with very high coverage
illumina_primers <- search_16s_primers(
  platform = "Illumina", 
  coverage = "Very High"
)

# Find primers for marine studies
marine_primers <- search_16s_primers(keyword = "marine")

# Find Earth Microbiome Project primers
emp_primers <- search_16s_primers(reference = "Caporaso")

# Complex search: V3-V4 region, low bias, for Illumina
optimal <- search_16s_primers(
  region = "V3-V4",
  bias_level = "Very Low",
  platform = "Illumina"
)
}
}
