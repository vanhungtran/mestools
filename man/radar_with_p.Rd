% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radarplot.R
\name{radar_with_p}
\alias{radar_with_p}
\title{Create Radar Plot with Statistical Tests}
\usage{
radar_with_p(
  data,
  value_vars,
  group_var = NULL,
  mode = c("auto", "groups", "items"),
  items_layout = c("grid", "single", "overlay_all"),
  item_name = NULL,
  show_average = TRUE,
  engine = c("ggplot", "fmsb"),
  p_method = c("auto", "t.test", "anova", "none"),
  max_scale = NULL,
  colors = NULL,
  title = NULL,
  text_radius = 1.35,
  mfrow = NULL,
  facet_ncol = 4,
  show_ci = TRUE,
  ci_level = 0.95,
  ci_alpha = 0.15
)
}
\arguments{
\item{data}{Data frame containing the data to plot}

\item{value_vars}{Character vector of column names to plot on radar axes}

\item{group_var}{Character. Grouping variable name (only for mode="groups")}

\item{mode}{Character. One of "auto", "groups", or "items". Auto-detects based on group_var}

\item{items_layout}{Character. For items mode: "grid" (one radar per item),
"single" (one chosen item), or "overlay_all" (all items on one radar)}

\item{item_name}{Character. For items_layout="single", specify which item to plot}

\item{show_average}{Logical. Show average line in items mode}

\item{engine}{Character. For groups mode: "ggplot" or "fmsb"}

\item{p_method}{Character. Statistical test: "auto", "t.test", "anova", or "none"}

\item{max_scale}{Numeric. Maximum value for radar axis scale}

\item{colors}{Character vector of colors for groups/items}

\item{title}{Character. Plot title}

\item{text_radius}{Numeric. For fmsb groups mode: p-value text radius}

\item{mfrow}{Numeric vector. Items grid layout, e.g., c(3,4)}

\item{facet_ncol}{Integer. Facet columns (unused, placeholder)}

\item{show_ci}{Logical. Show confidence intervals for groups mode (default: TRUE)}

\item{ci_level}{Numeric. Confidence level for intervals (default: 0.95)}

\item{ci_alpha}{Numeric. Transparency for CI ribbons in ggplot (default: 0.15)}
}
\value{
Invisibly returns a list containing plot object (if ggplot), p-values,
means, confidence intervals, engine type, mode, test type, and max_scale
}
\description{
Creates radar (spider) charts with optional statistical testing and p-values.
Supports two modes: groups mode (with statistics) and items mode (without statistics).
}
\examples{
\dontrun{
# Groups mode with t-test and confidence intervals
data <- data.frame(
  group = rep(c("Control", "Treatment"), each = 20),
  var1 = rnorm(40, 5, 2),
  var2 = rnorm(40, 7, 2),
  var3 = rnorm(40, 6, 2)
)

# With 95\% confidence intervals (default)
result <- radar_with_p(
  data = data,
  value_vars = c("var1", "var2", "var3"),
  group_var = "group",
  mode = "groups",
  engine = "ggplot",
  p_method = "auto",
  show_ci = TRUE
)

# With custom 99\% confidence intervals
result <- radar_with_p(
  data = data,
  value_vars = c("var1", "var2", "var3"),
  group_var = "group",
  mode = "groups",
  engine = "fmsb",
  show_ci = TRUE,
  ci_level = 0.99
)

# Without confidence intervals
result <- radar_with_p(
  data = data,
  value_vars = c("var1", "var2", "var3"),
  group_var = "group",
  mode = "groups",
  show_ci = FALSE
)

# Items mode - grid layout
items_data <- data.frame(
  item1 = c(5, 7, 6, 8, 5),
  item2 = c(7, 6, 8, 7, 6),
  item3 = c(6, 8, 7, 9, 7)
)
rownames(items_data) <- paste("Item", 1:5)

radar_with_p(
  data = items_data,
  value_vars = colnames(items_data),
  mode = "items",
  items_layout = "grid",
  show_average = TRUE
)
}
}
